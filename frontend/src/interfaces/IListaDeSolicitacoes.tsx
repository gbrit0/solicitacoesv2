export default interface IListaDeSolicitacoes {
  id: string;
  status: string;
  requester: string;
  product: string;
  quantity: number;
  requestDate: string;
  needDate: string;
  observations: string;
 }